server:
  port: 8604

spring:
  application:
    name: mogu-sms
  cloud:
    nacos:
      username: nacos
      password: wy0723
      discovery:
        server-addr: localhost:8848
        namespace: prod
      config:
        server-addr: localhost:8848
        file-extension: yaml
        #指定分组
        group: prod
        #指定命名空间
        namespace: prod

    # 配置Sentinel流控
    sentinel:
      transport:
        #配置Sentinel dashboard地址
        dashboard: localhost:8070
        #默认8719端口,如果被占用会向上扫描。
        port: 8719
